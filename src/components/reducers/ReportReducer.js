var init = {
    country = "",
    summary = {},
    data = {}
}

const reportReducer = (state=init, action) => {
    var nextState = {...state};
    switch (key) {
        case value:
            
            break;
    
        default:
            return nextState;
    }
    return nextState;
}